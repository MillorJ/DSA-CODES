// no 1.) Given an array of integers and the actual size of the array as parameters, the function will return all the even numbers, with -1 being the last element to the calling function

arr {} = 1,2,3,4,6



// no 2.) Create a function that will ask for user input string that will return a all vowels to the calling function.

//Given A and B as array of size 2, and represents a fraction where index 0 and index 1 are numerator and denominator, respectively the function will multiply the fraction and return the product to the calling function.

A[2] = {1, 2}
B[2] = {4, 8}